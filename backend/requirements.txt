# T.I.M. backend worker — Python dependencies.
# Install into a venv:  python -m venv .venv && .venv/Scripts/activate
#                       pip install -r requirements.txt

# WebSocket server
websockets==12.0

# Voice Activity Detection
onnxruntime==1.17.0     # Silero VAD inference

# Speaker biometric (ECAPA-TDNN). Uncomment when ready to enrol:
# speechbrain==1.0.0
# torch==2.2.0
# torchaudio==2.2.0

# Speech-to-Text (Whisper.cpp Python binding)
# faster-whisper==0.10.0
# whisper-cpp-python==0.2.0

# Text-to-Speech (Piper)
# piper-tts==1.2.0

# Vision (Moondream2 — runs via ONNX or transformers)
# onnxruntime-gpu==1.17.0   # if CUDA available
# transformers==4.38.0
# Pillow==10.2.0

# Video pipeline (FFmpeg subprocess — only needs the binaries)
# Place ffmpeg.exe + ffprobe.exe next to audio_server.py

# SearXNG scraper (just httpx — SearXNG itself runs as a separate service)
httpx==0.26.0

# Utilities
numpy==1.26.3
pydantic==2.6.0
