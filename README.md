# T.I.M. — This Is Me  (Master Blueprint v0.2)

A **100% offline, privacy-first, E2EE** AI mentor compiled as a native
Windows `.exe`. T.I.M. learns exclusively from you, forces you to confront
your flaws, corrects your communication, and secures your data with
military-grade encryption.

> Generated by `build_tim.py`. Re-run that script to refresh the workspace
> tree and the `TIM_Project_Workspace.zip` deliverable.

## Architecture (7 phases)

```
 ┌───────────────────────────────────────┐        ┌───────────────────────────────┐
 │  Flutter Desktop (Windows .exe)       │  WS    │  Python Worker (audio+vision) │
 │  - Gemini UI + Live Call overlay      │ ─────► │  - asyncio + websockets       │
 │  - Riverpod state                     │  PCM   │  - Silero VAD (800ms)         │
 │  - Supabase multi-tenant auth         │ ◄────── │  - Whisper STT                │
 │  - llama_cpp_dart FFI (in-process)    │  TTS   │  - Piper TTS                  │
 │  - Local vault: SQLite + sqlite-vec   │        │  - SpeechBrain ECAPA-TDNN     │
 │  - AES-256-GCM E2EE                   │        │  - Moondream2 screen vision   │
 │  - Hardware profiler (RAM/VRAM/Batt)  │        │  - FFmpeg video frames        │
 │  - .gguf auto-downloader              │        │  - SearXNG job scraper        │
 │  - Genesis onboarding (Memory Blocks) │        └───────────────────────────────┘
 │  - Drag-drop multimodal input         │
 │  - Reflexion self-correction          │
 └──────────────┬────────────────────────┘
                │ AES-256-GCM ciphertext blobs (only if Cloud Sync ON)
                ▼
        ┌──────────────────┐
        │  Supabase        │   Multi-tenant: owner / wife / sister / friends
        │  - auth.users    │   Each user = isolated profile + isolated vault.
        │  - memory_blobs  │   Supabase NEVER sees plaintext. Stores ciphertext
        │  (encrypted)     │   only. Recovery requires the master password.
        └──────────────────┘
```

## Repo layout

```
TIM_Project_Workspace/
├── pubspec.yaml
├── lib/
│   ├── main.dart, app.dart
│   ├── core/                 # theme, config, logger, constants
│   ├── data/
│   │   ├── services/         # supabase, websocket, local_vault, crypto,
│   │   │                     # llm_engine, model_downloader, speech_analytics,
│   │   │                     # screen_watcher, video_pipeline, job_scraper,
│   │   │                     # reflexion_engine, tim_backup
│   │   ├── models/           # memory, file_chip, voice_frame, learned_rule
│   │   └── repositories/     # memory_repository (sqlite-vec), directives_repo
│   ├── platform/
│   │   └── channels/         # hardware_scanner (RAM/VRAM/Battery via DXGI)
│   └── presentation/
│       ├── providers/        # auth, vault, chat, voice, onboarding,
│       │                     # hardware, model, sync
│       ├── screens/          # login, home, genesis, live_call
│       └── widgets/          # sidebar, chat_input, message_bubble,
│                             # voice_indicator, memory_block, file_chip,
│                             # waveform, override_modal, drag_drop_zone
├── backend/                  # Python audio + vision + scraper worker
│   ├── audio_server.py       # main WS loop, VAD, biometric gate, STT/TTS
│   ├── vad.py                # Silero VAD (energy fallback)
│   ├── voice_biometric.py    # SpeechBrain ECAPA-TDNN (10s enrolment)
│   ├── stt_whisper.py        # Whisper STT wrapper
│   ├── tts_piper.py          # Piper TTS wrapper
│   ├── screen_vision.py      # Moondream2 wrapper (voice-triggered)
│   ├── video_pipeline.py     # FFmpeg frame extraction (1 frame / 3s)
│   ├── job_scraper.py        # SearXNG client
│   ├── reflexion_worker.py   # session-end summariser (called by router)
│   └── requirements.txt
├── supabase/
│   ├── migrations/0001_init.sql   # auth + encrypted memory_blobs ONLY
│   ├── config.toml
│   └── .env.example
└── assets/
```

## Phase-by-phase status

| Phase | Module                              | Status            |
|-------|-------------------------------------|-------------------|
| 1     | Gemini UI + sidebar                 | Implemented       |
| 1     | Live Call View (waveform overlay)   | Implemented       |
| 1     | Multi-tenant Supabase login         | Implemented       |
| 1     | Drag-and-drop universal input       | Implemented       |
| 2     | llama_cpp_dart FFI engine           | Wired (drop in GGUF) |
| 2     | Hardware profiler (RAM/VRAM/Batt)   | Implemented (Dart + C++) |
| 2     | .gguf auto-downloader               | Implemented       |
| 2     | Safety override modal               | Implemented       |
| 3     | Genesis onboarding screen           | Implemented       |
| 3     | Resume → Memory Blocks extraction   | Wired (LLM-extracted) |
| 3     | Confirm-before-vectorise dashboard  | Implemented       |
| 4     | Local SQLite + sqlite-vec vault     | Implemented       |
| 4     | AES-256-GCM E2EE (PBKDF2 from pwd)  | Implemented       |
| 4     | Encrypted Supabase blob sync        | Implemented       |
| 4     | Export/Import .tim backup           | Implemented       |
| 5     | VAD (800ms) + Whisper STT           | Implemented        |
| 5     | Piper TTS streaming                 | Implemented (stub) |
| 5     | Speech analytics (filler/pitch/gap) | Implemented       |
| 5     | ECAPA-TDNN biometric barge-in       | Stubbed (open mode) |
| 6     | PDF text extraction                 | Implemented (syncfusion) |
| 6     | Voice-triggered screen watcher      | Wired (Moondream2 stub) |
| 6     | FFmpeg video frame extraction       | Implemented (backend) |
| 7     | SearXNG job scraper                 | Implemented (stub) |
| 7     | Reflexion self-correction loop      | Implemented       |

## Setup

### 1. Front-end (Flutter Desktop)

```bash
cd TIM_Project_Workspace
flutter create . --platforms=windows
flutter pub get
flutter run -d windows \
  --dart-define=SUPABASE_URL=https://YOUR-PROJECT.supabase.co \
  --dart-define=SUPABASE_ANON_KEY=YOUR-ANON-KEY \
  --dart-define=WS_URL=ws://127.0.0.1:8765
```

### 2. Audio / vision / scraper worker (Python)

```bash
cd TIM_Project_Workspace/backend
python -m venv .venv
.venv/Scripts/activate             # Windows
pip install -r requirements.txt
python audio_server.py --host 127.0.0.1 --port 8765
```

### 3. Supabase (auth + encrypted blob sync only)

```bash
cd TIM_Project_Workspace
supabase db push                   # applies migrations/0001_init.sql
```

## TTS Directive (built-in)

The seed enforces that the TTS engine **always** pronounces **Q-L-U-E** as
the spoken word **"clue"**. This rule lives in the local `directives` table
as a `tts` directive and is applied by the TTS layer before synthesis.

## Iteration Hints for the Antigravity IDE Agent

1. Replace `backend/audio_server.py::pipeline_utterance()` with a real
   Whisper.cpp → llama_cpp_dart → Piper chain.
2. Implement `backend/voice_biometric.py::_load_speechbrain()` once the
   SpeechBrain ECAPA-TDNN weights are provisioned.
3. Implement the Windows side of `tim.hardware/scanner` (with DXGI VRAM
   probe) in `windows/runner/flutter_window.cc` — see
   `windows/runner/README.md`.
4. Bundle `ffmpeg.exe` + `ffprobe.exe` into `backend/` (downloaded by the
   Antigravity agent or shipped via MSIX assets).
5. Wire `lib/data/services/job_scraper.dart` to a local SearXNG instance
   (`http://localhost:8080`).

— Generated by `build_tim.py`.
